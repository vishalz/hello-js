

/* 
 * Declaring a function
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/function
 *@todo create a function called doubleIt using a function decalration

 *
 *
 * @todo define a function with a name squareIt that takes a number as an argument and returns the sqaure of the number.
 * @todo define a function with a name addThem that takes two  numbers as an argument and returns the sum of the numbers.
 * @todo define a function with a name  bumpIt that takes a number n as an argument and returns a n+1 increme it by one and returns
 * 
 * Passing primitive types by Value
 * @todo declare a variable myAge and assign it number value 9. Call function bump  pass myAge as argument to function bump
 * @todo print the return value of bump
 * @todo prin the value of myAge
 *

 * Testing square with mocha test/test-sqaure.js
 * @todo It should take a number value 10 as an argument  and return a number value 100 that is the sqare of two argumnts
 * @todo It should take boolean true value as argument and return a number value that is equal to 1 
 * @todo it should take boolean false value as an argument and return a number value that is equal to 0 
 * @todo it should take a string value "foo" as an argument and return a number value = NaN
 *
 *
 *
 *  * @todo define a function called opposite that takes a boolean value as an argument and returns the logical not 
 * @todo declare a variable  iAmGood and assign a value true. Call opposite and pass iAmGood as argument
 * @todo print the return value of opposite
 * @todo print the value of iAmGood
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */
